export enum Status {
  INACTIVE,
  ACTIVE,
}

export enum MarketingType {
  ALL,
  BRAND,
  WEBSITE,
  MARKETING,
}

export enum BussinessStage {
  STARTUP,
  EALY_STAGE,
  ESTABLISHED,
}

export enum DocumentType {
  TERMS_CONDITION,
  PRIVACY_POLICY,
}

export enum Role {
  ADMIN,
  USER,
  SUBADMIN,
}

export enum ContactPurpose {
  WEB_DESIGN,
  DEVELOPMENT,
  MOBILE_APP_DESIGN,
  OTHERS,
}
